'use strict';

describe('Directive: bsHasError', function () {

  // load the directive's module
  beforeEach(module('tisostengoApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
